// timing.c — реализация находится в static inline-функциях timing.h.
// Этот файл нужен только для того, чтобы существовал объектный модуль
// при сборке по Makefile (SRC_CORE включает timing.c).

#include "timing.h"
